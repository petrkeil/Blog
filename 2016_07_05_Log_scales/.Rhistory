if(ylog)
{
ybreaks <- ylim[1]:ylim[2]
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE,...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
}
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=TRUE,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xbreaks <- xlim[1]:xlim[2]
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
}
if(ylog)
{
ybreaks <- ylim[1]:ylim[2]
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE,...)
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=TRUE,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xbreaks <- xlim[1]:xlim[2]
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
}
if(ylog)
{
ybreaks <- ylim[1]:ylim[2]
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
#for(x in xbase^xbreaks)
#{
#  subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
#  abline(v=subx, col="grey")
#}
#for(y in ybase^ybreaks)
#{
#  suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
#  abline(h=suby, col="grey")
#}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=TRUE,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xbreaks <- xlim[1]:xlim[2]
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
}
if(ylog)
{
ybreaks <- ylim[1]:ylim[2]
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
#for(x in xbase^xbreaks)
#{
#  subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
#  abline(v=subx, col="grey")
#}
#for(y in ybase^ybreaks)
#{
#  suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
#  abline(h=suby, col="grey")
#}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
xbreaks <- xlim[1]:xlim[2]
xlim[1] <- floor(log(xlim[1], base=xbase))
0.001
log10(0.001)
floor(log10(0.001))
xbreaks <- xlim[1]:xlim[2]
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xlim
xlim=c(0.01,1000
ylim=c(0.3,1000)
ylim=c(0.3,1000)
xbreaks <- xlim[1]:xlim[2]
xbreaks <- xlim[1]:xlim[2]
xlim
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=TRUE,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
#for(x in xbase^xbreaks)
#{
#  subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
#  abline(v=subx, col="grey")
#}
#for(y in ybase^ybreaks)
#{
#  suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
#  abline(h=suby, col="grey")
#}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=TRUE,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=TRUE,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
}
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=TRUE,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
}
else axis(side=1, at=xbreaks, labels=xbreaks, tck=0.02)
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
}
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=F,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
}
else axis(side=1, at=xbreaks, labels=xbreaks, tck=0.02)
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
}
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=F,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
}
else axis(side=1, at=xlim, labels=xlim, tck=0.02)
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
}
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
seq(0.1, 1000, by=5)
seq(0.1, 1000)
?seq
seq(0.1, 1000, length.out=5)
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=F,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
}
else
{
xlim <- seq(xlim[1], xlim[2], length.out=5)
axis(side=1, at=xlim, labels=xlim, tck=0.02)
}
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
else
{
ylim <- seq(ylim[1], ylim[2], length.out=5)
axis(side=1, at=ylim, labels=ylim, tck=0.02)
}
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
?axis
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=F,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
}
else
{
#xlim <- seq(xlim[1], xlim[2], length.out=5)
#axis(side=1, at=xlim, labels=xlim, tck=0.02)
axis(side=1, tck=0.02)
}
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
else
{
ylim <- seq(ylim[1], ylim[2], length.out=5)
axis(side=1, at=ylim, labels=ylim, tck=0.02)
}
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=F,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
}
else  axis(side=1, tck=0.02)
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
else  axis(side=1, tck=0.02)
}
loglog.plot <- function(xlim=c(0.01,1000), xbase=10, xlog=TRUE,
ylim=c(0.3,1000), ybase=2, ylog=TRUE, ...)
{
if(xlog)
{
xlim[1] <- floor(log(xlim[1], base=xbase))
xlim[2] <- round(log(xlim[2], base=xbase))
xbreaks <- xlim[1]:xlim[2]
}
if(ylog)
{
ylim[1] <- floor(log(ylim[1], base=ybase))
ylim[2] <- round(log(ylim[2], base=ybase))
ybreaks <- ylim[1]:ylim[2]
}
plot(xlim, ylim, type="n", axes=FALSE, frame=TRUE, ...)
if(xlog)
{
for(x in xbase^xbreaks)
{
subx <- log(seq(from=x, to=x*xbase, length=xbase) , base=xbase )
abline(v=subx, col="grey")
}
axis(side=1, at=xbreaks, labels=xbase^xbreaks, tck=0.02)
}
else  axis(side=1, tck=0.02)
if(ylog)
{
for(y in ybase^ybreaks)
{
suby <- log(seq(from=y, to=y*ybase, length=ybase), base=ybase)
abline(h=suby, col="grey")
}
axis(side=2, at=ybreaks, labels=ybase^ybreaks, las=2, tck=0.02)
}
else  axis(side=1, tck=0.02)
}
loglog.plot(xlab="log with base of 10", ylab="log with base of 2")
?plot
?xlim
